# Runtime-only dependencies for the API server.
# Heavy dev/seeding deps (spacy, playwright, geopandas, pandas, scikit-learn)
# are intentionally excluded — they are only needed for one-time setup scripts.

# API
fastapi==0.115.0
uvicorn[standard]==0.30.0
python-multipart==0.0.9

# Database
sqlalchemy[asyncio]==2.0.35
asyncpg==0.29.0
alembic==1.13.3
geoalchemy2==0.15.2
psycopg2-binary==2.9.9

# Config
pydantic-settings==2.4.0
python-dotenv==1.0.1

# Background jobs
celery[redis]==5.4.0
redis==5.0.8

# HTTP & scraping
httpx==0.27.2
beautifulsoup4==4.12.3
lxml==5.3.0
feedparser==6.0.11
